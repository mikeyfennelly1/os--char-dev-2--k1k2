= k1

== Purpose

To receive input from a GPIO pin in a raspberry pi, then publish some data to a named pipe.

== Build and insert

=== Build via make
[source, bash]
----
sudo upt update
sudo make
----

=== Insert module

[source, bash]
----
sudo insmod main.ko
----
